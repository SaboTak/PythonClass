#Pedirle un dato al usuario
numero = input('Dame tu nombre: ')

#Convertimos el numero a entero para poder operar aritmeticamente con el
resultado = int(numero) * 2

#Convertimos el numero a flotante para poder operar aritmeticamente con el
resultado = float(numero) * 2

#Mostrando el dato
print(f'El cuadrado de tu numero es: {resultado}')